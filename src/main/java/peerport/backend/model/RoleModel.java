package peerport.backend.model;

// Used to keep track of user roles
public class RoleModel {
    // Enum for predefined roles
    public enum Role {
        STUDENT,
        INSTRUCTOR,
        ADMIN
    }
}
